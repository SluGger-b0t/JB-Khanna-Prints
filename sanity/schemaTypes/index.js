import product from './product'
import premiumProduct from './premiumProduct'
import category from './category'

export const schema = {
  types: [product, premiumProduct, category],
}

export const schemaTypes = [premiumProduct, category]
